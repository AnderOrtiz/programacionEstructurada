
//
let edad = 20;
let tieneDui = false;

let puedeVotar = (edad >= 18 && tieneDui == true) ? "Puede votar" : "No puede votar";
console.log(puedeVotar);

// Mayor de 30 y Tener Membresia

let age = 35;
let esMiembro = false
let descuento = (age >= 30 || esMiembro) ? "Se le aplica descuento" : "No se le aplica descuento"
console.log(descuento);

let personaedad =  16
let altura = 1.80
let rusa = (persona > 11 && altura > 1.60)? "Puede pasar a la rusa": "No puede pasar a la rusa";